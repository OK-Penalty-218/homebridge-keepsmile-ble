{
  "pluginAlias": "Homebridge Keepsmile BLE",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Platform Name",
        "type": "string",
        "default": "Keepsmile BLE"
      },
      "devicename": {
        "title": "Accessory Name",
        "type": "string",
        "default": "Light Strip"
      },
      "bluetoothuuid": {
        "title": "Bluetooth UUID (Optional)",
        "type": "string",
        "default": "",
        "description": "Leave empty for automatic discovery."
      }
    }
  }
}
