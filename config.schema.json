{
  "pluginAlias": "Homebridge Keepsmile BLE",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Platform Name",
        "type": "string",
        "default": "Keepsmile BLE"
      },
      "devicename": {
        "title": "Accessory Name",
        "type": "string",
        "required": true,
        "default": "Light Strip"
      },
      "bluetoothuuid": {
        "title": "Bluetooth UUID",
        "type": "string",
        "required": false   // This can now be optional, based on your discovery flow
      }
    }
  }
}
